# Interim Cycle-5 build: chain-contract features disabled (Plan 27).
# Local `pnpm dev` matches the interim build so the dead world is never polled during Cycle 5.
# Plan 28 sets this to true (or deletes this file) when the contracts are re-published.
VITE_CHAIN_ENABLED=false
